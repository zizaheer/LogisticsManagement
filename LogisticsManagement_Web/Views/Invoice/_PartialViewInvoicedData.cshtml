@using LogisticsManagement_Web.Models;
@model IEnumerable<ViewModel_GeneratedInvoice>


<script>
    $(document).ready(function () {
        if ($.fn.dataTable.isDataTable('#invoiced-list')) {
            $('#invoiced-list').DataTable().destroy();
        }
        $('#invoiced-list').DataTable({
            "order": [[0, "desc"]],
            "lengthChange": false,
            searching: false,
            paging: false,
            info: false
        });
    });
</script>

<div class="row" style="max-height:230px; overflow-y:scroll;">
    <div class="col-sm-12">
        <table id="invoiced-list" class="table table-sm table-striped lms_dataTable" cellspacing="0" style="width:100%; margin-top: -1px !important; font-size:.75em">
            <thead>
                <tr>
                    <th>W/B #</th>
                    <th>Dispatched to</th>
                    <th>Delivery date</th>
                    <th>Received by</th>
                    <th>Delivery note</th>
                    <th>Consignee address</th>

                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @*@foreach (var item in Model.OrderByDescending(c => c.PickupDatetime))
                {
                    <tr>
                        <td>
                            @item.WayBillNumber
                        </td>
                        <td>
                            @item.DispatchedToEmployeeName (@item.DispatchedToEmployeeId)
                        </td>
                        <td>
                            @item.DeliverDatetime
                        </td>
                        <td>
                            @item.ReceivedByName
                        </td>
                        <td>
                            @item.ProofOfDeliveryNote
                        </td>
                        <td>
                            @item.ConsigneeAddress
                        </td>

                        <td>
                            <button id="btnEdit" data-waybillnumber='@item.WayBillNumber' class="btn btn-sm btn-primary btnEdit" type="button"><i class="fa fa-edit"></i> </button>
                            <button id="btnDelete" data-waybillnumber='@item.WayBillNumber' class="btn btn-sm btn-danger btnDelete" data-toggle="modal" data-target="#alert-modal" type="button"><i class="fa fa-trash"></i> </button>
                        </td>
                    </tr>
                }*@


            </tbody>
        </table>
    </div>
</div>
<script src="~/js/invoice.js"></script>
